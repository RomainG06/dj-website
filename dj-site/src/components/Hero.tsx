import React from 'react';

const Hero: React.FC = () => {
    return (
        <section id="hero" className="hero">
            <h1>DJ Diaz</h1>
            <p>Les meilleurs sets pour vos soirées</p>
            <button>Écouter maintenant</button>
        </section>
    );
};

export default Hero;
